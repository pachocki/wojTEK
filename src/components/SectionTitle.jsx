import React from 'react'

const SectionTitle = ({title}) => {
  return (
    <div className=" text-7xl font-semibold text-center  md:text-5xl sm:text-4xl xs:text-3xl">
        <h2>{title}</h2>
      </div>
  )
}

export default SectionTitle